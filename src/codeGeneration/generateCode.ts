import {getAppInfo} from '../constants/getAppInfo'
import {generateAppCode} from './generateAppCode'
import {insertAddedCode} from './insertAddedCode'
import {storeAddedCode} from './storeAddedCode'

export async function generateCode(appFile, appDir: string, jsonPath: string) {
}
